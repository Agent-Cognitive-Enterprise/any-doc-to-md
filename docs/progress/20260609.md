# 2026-06-09 — Handoff Discipline

## Slice: Add HANDOFF.md Session Discipline

**Status:** complete
**Date:** 2026-06-09
**Role:** build

**Objective:**
Add repository instructions and a root handoff snapshot so future ADTM agent sessions know to read and update `HANDOFF.md`.

**Files touched:**

- `AGENTS.md`
- `HANDOFF.md`
- `docs/progress/20260609.md`

**Implemented changes:**

- Added `HANDOFF.md` to the required read-before-writing flow when present.
- Added rules for creating, maintaining, compressing, and timestamping `HANDOFF.md`.
- Added the repository-standard handoff section template.
- Clarified that SABRE means Slice-Based Adversarial Build/Review Engineering.
- Connected change-producing SABRE slices to `docs/progress/YYYYMMDD.md`, while keeping `HANDOFF.md` as the current-state snapshot.
- Created the initial root `HANDOFF.md`.

**Verification:**

- `git diff --check -- AGENTS.md`: passed.
- `rg -n "[[:blank:]]$" AGENTS.md HANDOFF.md docs/progress/20260609.md`: no matches.
- `python -m pytest -q`: not run; this slice changed only process Markdown files.

**Open follow-ups:**

- None planned for this slice after verification.

**Suggested next slice:**

Resume or review the existing paragraph-continuity repair workstream if that remains the active objective.

## Slice: Fix Red-Team Findings

**Status:** complete
**Date:** 2026-06-09
**Role:** build

**Objective:**
Fix the red-team findings against the uncommitted handoff-discipline and paragraph-repair changes.

**Files touched:**

- `AGENTS.md`
- `HANDOFF.md`
- `docs/progress/20260609.md`
- `src/anydoc2md/paragraph_repair/repairer.py`
- `tests/test_paragraph_repair_repairer.py`

**Implemented changes:**

- Added direct repairer tests for row-sliced merges, complete-paragraph non-merges, heading/list/table/code/image/caption boundaries, max merged length, safe hyphen joins, bounded examples, and the short-run collapse guard.
- Changed `repair_blocks()` so collected groups shorter than `min_continuation_run_blocks` are left untouched.
- Adjusted repairer content-preservation normalization so safe lowercase discretionary hyphen joins remain content-preserving.
- Clarified that read-only Q&A and red-team review may leave `HANDOFF.md` untouched to avoid mutating the diff being reviewed.

**Verification:**

- `python -m pytest tests/test_paragraph_repair_repairer.py -q`: 11 passed.
- `python -m pytest tests/test_paragraph_repair_model.py tests/test_paragraph_repair_detector.py tests/test_paragraph_repair_markdown_blocks.py tests/test_paragraph_repair_repairer.py -q`: 75 passed.
- `python -m pytest -q`: 525 passed.
- `git diff --check`: passed.
- `rg -n "[[:blank:]]$" AGENTS.md HANDOFF.md docs/progress/20260609.md src/anydoc2md/paragraph_repair/repairer.py tests/test_paragraph_repair_repairer.py`: no matches.

**Open follow-ups:**

- None for the red-team findings fixed in this slice.

**Suggested next slice:**

Review or implement the paragraph-repair quality gate after full verification passes.

## Slice: Preserve Hyphen Evidence

**Status:** complete
**Date:** 2026-06-09
**Role:** build

**Objective:**
Fix follow-up red-team findings that ambiguous de-hyphenation could corrupt real compounds and that `content_preserved` could not detect that corruption.

**Files touched:**

- `HANDOFF.md`
- `docs/progress/20260609.md`
- `src/anydoc2md/paragraph_repair/model.py`
- `src/anydoc2md/paragraph_repair/repairer.py`
- `tests/test_paragraph_repair_repairer.py`

**Implemented changes:**

- Changed hyphen joins to preserve the trailing hyphen and join without a space instead of deleting the hyphen.
- Removed the discretionary-hyphen normalization from `content_preserved`; the check now compares strict whitespace tokens.
- Added regressions proving wrapped compounds remain hyphenated and do not become `state-of-theart`, `wellknown`, or `longterm`.
- Added a repairer-path regression where a hyphen join reports `content_preserved=False`.
- Added `pair_is_continuation()` for the pair-level decision and kept `should_merge()` as a documented compatibility alias.
- Advanced past collected short near-runs to avoid repeatedly rescanning the same forward run.

**Verification:**

- `python -m pytest tests/test_paragraph_repair_repairer.py -q`: 12 passed.
- `python -m pytest tests/test_paragraph_repair_model.py tests/test_paragraph_repair_detector.py tests/test_paragraph_repair_markdown_blocks.py tests/test_paragraph_repair_repairer.py -q`: 76 passed.
- `python -m pytest -q`: 526 passed.
- `git diff --check`: passed.
- `rg -n "[[:blank:]]$" AGENTS.md HANDOFF.md docs/progress/20260609.md src/anydoc2md/paragraph_repair/model.py src/anydoc2md/paragraph_repair/repairer.py tests/test_paragraph_repair_model.py tests/test_paragraph_repair_repairer.py`: no matches.

**Open follow-ups:**

- None for the follow-up red-team findings fixed in this slice.

**Suggested next slice:**

Review or implement the paragraph-repair quality gate after full verification passes.

## Slice: Orthogonal Content-Preservation Evidence

**Status:** complete
**Date:** 2026-06-09
**Role:** build

**Objective:**
Fix the residual red-team finding that `content_preserved` is a single document-level boolean: one ambiguous hyphen-wrap taints the whole `RepairDraft`, so a gate that hard-rejects on `content_preserved=False` can never auto-accept a document containing any hyphen join.

**Files touched:**

- `src/anydoc2md/paragraph_repair/model.py`
- `src/anydoc2md/paragraph_repair/repairer.py`
- `tests/test_paragraph_repair_model.py`
- `tests/test_paragraph_repair_repairer.py`

**Implemented changes:**

- Redefined `content_preserved` as a whitespace-insensitive character round-trip (`"".join(text.split())` equality). It is a true loss guard: `True` for character-preserving space and hyphen joins, `False` only on real content loss (e.g. the old hyphen-dropping bug).
- Added `RepairDraft.hyphen_join_count`, counted in `_merge_group`, so the ambiguity of collapsed end-of-block hyphen boundaries is reported orthogonally instead of overloading the loss boolean.
- Documented the `_merge_group` right-edge invariant that makes recomputing the join kind during the fold safe.
- Updated the two hyphen tests to the new semantics (`content_preserved=True`, `hyphen_join_count` reported) and added an orthogonality test proving a clean run stays content-preserved when a hyphen run shares the document.
- Added a direct `_content_preserved` guard test demonstrating it ignores whitespace but catches a dropped character.

**Verification:**

- `python -m pytest tests/ -q -k paragraph_repair`: 78 passed.
- `python -m pytest -q`: 528 passed.
- `git diff --check`: passed.
- `rg -n "[[:blank:]]$" HANDOFF.md docs/progress/20260609.md src/anydoc2md/paragraph_repair/model.py src/anydoc2md/paragraph_repair/repairer.py tests/test_paragraph_repair_model.py tests/test_paragraph_repair_repairer.py`: no matches.

**Open follow-ups:**

- When wiring the quality gate, treat `content_preserved=False` as real loss (reject) and `hyphen_join_count > 0` as the review/scrutiny signal rather than an automatic reject.

**Suggested next slice:**

Review or implement the paragraph-repair quality gate using the orthogonal `content_preserved` / `hyphen_join_count` signals.
